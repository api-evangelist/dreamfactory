naftiko: 1.0.0-alpha2
info:
  label: DreamFactory System API — User
  description: 'DreamFactory System API — User. 5 operations. Lead operation: DreamFactory List users. Self-contained Naftiko
    capability covering one Dreamfactory business surface.'
  tags:
  - Dreamfactory
  - User
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DREAMFACTORY_API_KEY: DREAMFACTORY_API_KEY
capability:
  consumes:
  - type: http
    namespace: system-user
    baseUri: https://{instance}/api/v2
    description: DreamFactory System API — User business capability. Self-contained, no shared references.
    resources:
    - name: system-user
      path: /system/user
      operations:
      - name: listusers
        method: GET
        description: DreamFactory List users
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createuser
        method: POST
        description: DreamFactory Create user
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: system-user-id
      path: /system/user/{id}
      operations:
      - name: getuser
        method: GET
        description: DreamFactory Get user
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updateuser
        method: PATCH
        description: DreamFactory Update user
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deleteuser
        method: DELETE
        description: DreamFactory Delete user
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: X-DreamFactory-Session-Token
      value: '{{env.DREAMFACTORY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: system-user-rest
    port: 8080
    description: REST adapter for DreamFactory System API — User. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/system/user
      name: system-user
      description: REST surface for system-user.
      operations:
      - method: GET
        name: listusers
        description: DreamFactory List users
        call: system-user.listusers
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createuser
        description: DreamFactory Create user
        call: system-user.createuser
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/system/user/{id}
      name: system-user-id
      description: REST surface for system-user-id.
      operations:
      - method: GET
        name: getuser
        description: DreamFactory Get user
        call: system-user.getuser
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: updateuser
        description: DreamFactory Update user
        call: system-user.updateuser
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteuser
        description: DreamFactory Delete user
        call: system-user.deleteuser
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: system-user-mcp
    port: 9090
    transport: http
    description: MCP adapter for DreamFactory System API — User. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: dreamfactory-list-users
      description: DreamFactory List users
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: system-user.listusers
      outputParameters:
      - type: object
        mapping: $.
    - name: dreamfactory-create-user
      description: DreamFactory Create user
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: system-user.createuser
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: dreamfactory-get-user
      description: DreamFactory Get user
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: system-user.getuser
      outputParameters:
      - type: object
        mapping: $.
    - name: dreamfactory-update-user
      description: DreamFactory Update user
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: system-user.updateuser
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: dreamfactory-delete-user
      description: DreamFactory Delete user
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: system-user.deleteuser
      outputParameters:
      - type: object
        mapping: $.
